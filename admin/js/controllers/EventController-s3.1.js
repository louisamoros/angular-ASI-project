'use strict';

angular.module('adminApp').controller('eventCtrl',eventCrtFnt);

loginCrtFnt.$inject=['$scope','$log', 'factory', '$window'];

function eventCrtFnt($scope, $log, factory, $window){ 
	
	$scope.savePres = function() {
	
	}; 

	$scope.newSlide = function() {
	
	}; 

}